# Quant Lab 项目规则

本文件约束所有在此仓库工作的 AI Agent 与开发者。

## 一、开发节奏

1. 不要一次性开发所有功能；每次只交付一个可运行、可测试的里程碑。
2. 所有阶段先写接口与测试边界，再实现功能。
3. 每完成一个阶段必须运行 `tests/` 中的全部测试。
4. 每次修改后汇报四件事：改了什么、为什么这样设计、如何测试、下一步是什么。
5. 不要为了“看起来完整”而增加没有实际作用的页面、接口或配置。

## 二、内容与量化边界

1. 严禁把历史回测收益描述成未来收益保证；教学页面统一展示风险提示。
2. 策略与课程内容必须区分：策略假设、公式、算法、参数、数据、回测结果。
3. 所有核心计算必须可解释，结果可追溯到具体数据、公式和计算过程。
4. 涉及回测时主动检查并防止：未来函数、数据泄露、幸存者偏差、过拟合、忽略交易成本、忽略滑点。
5. 未实现的模块只能以“后续里程碑”形式出现，不得放空表单或假计算。

## 三、模块边界

- `backend/quantlab/data`：市场数据读取与缓存。
- `backend/quantlab/indicators`：指标计算，只做纯函数。
- `backend/quantlab/strategies`：信号与策略规则。
- `backend/quantlab/backtest`：回测执行与成本建模。
- `backend/quantlab/analysis`：回测结果分析。
- `backend/quantlab/courses`：教学课程加载与校验。
- `backend/quantlab/progress`：学习进度存储。
- `backend/quantlab/api`：HTTP API 层，不包含计算逻辑。
- `frontend`：UI 层，只调用 API，不直接实现量化计算。

## 四、课程内容要求

每节正式课 JSON 必须包含：一句话解释、专业定义、类比、示例、公式、公式变量解释、算法步骤、Python 示例与说明、互动练习、小测验、常见错误、概念列表。

题目仅使用 `choice` 与 `numeric` 两种题型；答案键只保留在服务端，公开接口不得返回正确答案。
